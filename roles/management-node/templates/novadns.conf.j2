[default]
# The time between each nova run to get ips 
wait_time = 20

[openstack]
user = {{ openstack_os_user_name }}
tenant = {{ openstack_os_tenant_name }}
password = {{ openstack_os_password }}
auth_url = {{ openstack_os_auth_url }}